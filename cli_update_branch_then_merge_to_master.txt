git clone https://github.com/obrienke/first-repository.git
cd first-repository/
git checkout first-branch
# create a file
git add .
git commit -m "TEST NEW FILE ADDED TO BRANCH"
git push origin first-branch


git checkout master
git pull               
git merge first-branch #merges the changes from first branch to master.      
git push origin master 


# if you want to change the name of the user from git bash:
git config user.name "obrienke1"

git config user.name # will print out the name of the user.

